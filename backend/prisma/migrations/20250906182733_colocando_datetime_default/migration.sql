BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[T_PONTO] ADD CONSTRAINT [T_PONTO_PON_DT_PONTO_df] DEFAULT CURRENT_TIMESTAMP FOR [PON_DT_PONTO];

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
